import React from 'react';
import './SplashScreen.css';

const SplashScreen = () => {
    return (
        <div className="splash-screen">
            <h1>Levi's CineList</h1>
            {/* Vous pouvez ajouter ici une image, un logo ou une animation */}
        </div>
    );
};

export default SplashScreen;
